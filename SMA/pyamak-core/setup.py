from setuptools import setup, find_packages

setup(
    name='pyAmakCore',
    packages=find_packages(),
    version='0.1.2',
    description='AmakFramework in python',
    author='BE',
    install_requires=[],
)